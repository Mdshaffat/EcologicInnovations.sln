@model CaliforniumCore.Web.ViewModels.Shared.ErrorPageViewModel

@{
    ViewData["Title"] = ViewData["Title"] ?? "Page Not Found";
}

<section class="status-page-shell">
    <div class="container">
        <div class="status-page-card p-4 p-lg-5 text-center mx-auto" style="max-width: 760px;">

            <div class="status-page-icon mb-4">
                <i class="@Model.IconCssClass"></i>
            </div>

            <div class="status-page-code mb-2">@(Model.StatusCode?.ToString() ?? "404")</div>
            <div class="status-page-kicker mb-3">Page Not Found</div>

            <h1 class="display-6 status-page-title mb-3">@Model.Title</h1>

            @if (!string.IsNullOrWhiteSpace(Model.Message))
            {
                <p class="lead status-page-message mb-4">@Model.Message</p>
            }

            <div class="d-flex flex-wrap justify-content-center gap-2 mb-4">
                @if (!string.IsNullOrWhiteSpace(Model.PrimaryButtonText) && !string.IsNullOrWhiteSpace(Model.PrimaryButtonUrl))
                {
                    <a href="@Model.PrimaryButtonUrl" class="btn status-btn-primary">
                        <i class="bi bi-box-seam me-1"></i> @Model.PrimaryButtonText
                    </a>
                }
                @if (!string.IsNullOrWhiteSpace(Model.SecondaryButtonText) && !string.IsNullOrWhiteSpace(Model.SecondaryButtonUrl))
                {
                    <a href="@Model.SecondaryButtonUrl" class="btn status-btn-outline">
                        <i class="bi bi-house-door me-1"></i> @Model.SecondaryButtonText
                    </a>
                }
            </div>

            <hr class="status-page-divider" />

            <div class="mt-3">
                <p class="small fw-semibold mb-2" style="color: rgba(255,255,255,0.6);">Looking for something specific?</p>
                <ul class="list-unstyled small status-page-tips">
                    <li><i class="bi bi-check-circle me-1"></i> Double-check the URL for typos</li>
                    <li><i class="bi bi-search me-1"></i> Browse our product catalog</li>
                    <li><i class="bi bi-journal-text me-1"></i> Read our latest blog articles</li>
                    <li><i class="bi bi-envelope me-1"></i> <a href="@Url.Action("Index", "Contact")">Get in touch</a> if you need help</li>
                </ul>
            </div>
        </div>
    </div>
</section>
